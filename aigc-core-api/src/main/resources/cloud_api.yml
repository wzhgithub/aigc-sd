wx:
  cloud:
    feign:
      apache:
        maxConnections: 256
        maxConnPerRoute: 16
        requestTimeout: 10s
    config:
      host: "https://api.weixin.qq.com"
      clazz: com.yiyun.ai.core.api.business.wx.WXCloudAPI
      access-token: "xxx"
    db:
      sql:
        templates:
          Query: "db.collection(\"${collection}\").where(${condition}).limit(1).get()"
          Update: "db.collection(\"${collection}\").where(${condition}).update(${data})"
sd:
  config:
    host: "https://api.weixin.qq.com"
    clazz: com.yiyun.ai.core.api.business.sd.SDServerlessAPI